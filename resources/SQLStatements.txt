(Database: E-commerce)

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    password VARCHAR(100),
    user_name VARCHAR(100),
    email VARCHAR(100)
);

CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    total INT,
    status VARCHAR(100),
    user_id INT,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    price INT,
    description VARCHAR(100)
);

CREATE TABLE carts (
    id SERIAL PRIMARY KEY
);

CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    quantity INT,
    price INT,
    order_id INT,
    product_id INT,
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (product_id) REFERENCES products(id)
);

CREATE TABLE cart_items (
    id SERIAL PRIMARY KEY,
    product_id INT,
    cart_id INT,
    FOREIGN KEY (product_id) REFERENCES products(id),
    FOREIGN KEY (cart_id) REFERENCES carts(id)
);

----------------------------------------------------------------

INSERT INTO users (password, user_name, email)
VALUES ('test', 'testName', 'test@gmail.com'),
	   ('test2', 'testName2', 'test2@gmail.com');